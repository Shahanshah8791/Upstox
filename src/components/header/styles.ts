import {StyleSheet} from 'react-native';
import Colors from '../../constants/Colors';
export const styles = StyleSheet.create({
  headerContainer: {
    paddingHorizontal: 20,
    paddingVertical: 10,
    backgroundColor: Colors.color_75147C,
  },
  headerText: {
    fontSize: 16,
    fontWeight: '600',
    color: Colors.color_FFFFFF,
  },
});
